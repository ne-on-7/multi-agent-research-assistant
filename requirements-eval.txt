# Evaluation-only dependencies (kept out of the runtime image on purpose).
# Install alongside requirements.txt to run eval/run_eval.py:
#   pip install -r requirements.txt -r requirements-eval.txt
ragas>=0.2.0
datasets>=2.0.0
langchain-anthropic>=0.2.0
langchain-huggingface>=0.1.0
